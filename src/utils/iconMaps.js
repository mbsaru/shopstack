// src/utils/icon-map.js
// Import all Lucide icons you might want to use dynamically
import {
  ChevronRightIcon,
  ShoppingCart,
  Trash2,
  LogOut,
  Instagram,
  Mail,
  MessageSquare, // Often used for WhatsApp/SMS icon
  PlusCircle,
  MinusCircle,
  // ... add more icons as needed
} from "lucide-react";

export const IconMap = {
  ChevronRightIcon,
  ShoppingCart,
  Trash2,
  LogOut,
  Instagram,
  Mail,
  MessageSquare,
  PlusCircle,
  MinusCircle,
  // Add more as you import them
};

// Optional: A default icon if one isn't found
export const DefaultIcon = ChevronRightIcon;